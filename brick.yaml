name: hello_riverpod
description: A complete starter project for Flutter apps, with examples of navigation, requests and tests. The app uses riverpod, go_router, (hive), mockito, and follows the guidelines of Clean Architecture.

version: 0.1.0+1

environment:
  mason: ">=0.1.0-dev.26 <0.1.0"

vars:
  name:
    type: string
    description: .
    default: .
    prompt: First question ?
